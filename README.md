FlowSync Cursor Library v8 refined

Open index.html first.

Structure:
- index.html -> main index, all categories
- categories/*.html -> one page per category
- sources/*.html -> one separate HTML page per effect

Features:
- dark/light theme toggle
- category strip on top
- large effect boxes with text on one side and preview area on the other
- source button in bottom-right
- Coldboot watermark
